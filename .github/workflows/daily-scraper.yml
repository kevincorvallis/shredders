name: Daily Mountain Scraper

on:
  schedule:
    # Run at 6 AM and 6 PM UTC (10 PM and 10 AM PST)
    - cron: '0 6,18 * * *'

  # Allow manual triggering from GitHub UI
  workflow_dispatch:

jobs:
  scrape-mountains:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Scraper Endpoint
        run: |
          echo "🏔️ Starting mountain data scrape..."

          response=$(curl -s -w "\n%{http_code}" \
            -X GET \
            -H "Content-Type: application/json" \
            ${{ secrets.APP_URL }}/api/scraper/run)

          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')

          echo "Response Code: $http_code"
          echo "Response Body: $body"

          if [ "$http_code" -eq 200 ]; then
            echo "✅ Scraper completed successfully"

            # Parse JSON response to show stats
            successful=$(echo "$body" | grep -o '"successful":[0-9]*' | grep -o '[0-9]*')
            failed=$(echo "$body" | grep -o '"failed":[0-9]*' | grep -o '[0-9]*')

            echo "📊 Results: $successful successful, $failed failed"
          else
            echo "❌ Scraper failed with status $http_code"
            exit 1
          fi

      - name: Send Notification on Failure
        if: failure()
        run: |
          echo "❌ Mountain scraper failed. Check logs for details."
          # Add Slack/Discord notification here if desired
